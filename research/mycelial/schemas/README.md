# Lifecycle schemas

This directory contains the six research-only Phase 1 entity definitions for sites, survey sessions, lifecycle observations, media evidence, environmental snapshots, and state transitions. They are intentionally outside the production `schemas/` registry until a later admission review.
